# dobby-operator

Portable, OS-wipe-proof operator for Zen Lab.
Code-only repo. Runtime state lives outside the repo.

- Propose → Review → Approve → Execute → Log
- No marketplace installs
- Local-first by default; cloud models are opt-in for “Hatch mode”
